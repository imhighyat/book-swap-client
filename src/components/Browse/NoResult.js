import React from 'react';

export default function NoResult(){
	return(
		<div className='no-result'>
			<p>Sorry, your search did not return any result. Try again using another keyword or category.</p>
		</div>
	);
}
